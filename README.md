# proyecto_mercado

recomendaciones para instalar el codigo
-instalar appserv para apache/php y sql (creen su usuario root si no lo tienen, es recomendable que usen esto y no xampp pq con xampp hara incompatibilidad, pq python django requiere una version de sql que xampp no tiene)
-instalar python, pip y django si no lo tienen
-configuren el archivo settings en base a su usuario sql, en mi caso mi usuario es "root" y la contraseña 12345678
